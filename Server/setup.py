from setuptools import setup, find_packages

setup(
    name='ServerWeb',
    version='0.8',
    packages=[''],
    url='',
    license='',
    author='Ignac',
    author_email='',
    description='Server,server web',
    extras_require=dict(test=['pytest']),
    package_dir=find_packages(where='src'),

)
